<%= stylesheet_link_tag "invoice" %>

<div class="invoice-page">
  <%= form_for(invoice) do |f| %>
    <div class="invoice-header">
      <%= render partial: 'header_input', locals: { f: f} %>
    </div>

    <div class="invoice-main">
      <%= render partial: 'input', locals: { f: f} %>
    </div>
  <% end %>
</div>
